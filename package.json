{
  "name": "deepadata-edm-sdk",
  "version": "0.8.12",
  "description": "SDK for assembling EDM artifacts from user content. LLM-assisted extraction with interpretation constraints and profile support. Emitted artifacts stamp the installed edm-spec version.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "generate:spec": "node scripts/generate-spec-data.mjs",
    "build": "npm run generate:spec && tsc",
    "prepare": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "bundle-test": "node scripts/bundle-test.mjs",
    "clean": "rimraf dist"
  },
  "keywords": [
    "edm",
    "emotional-data-model",
    "deepadata",
    "llm",
    "extraction"
  ],
  "author": "DeepAData",
  "license": "UNLICENSED",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "edm-spec": "^0.8.3",
    "openai": "^6.17.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "peerDependencies": {
    "deepadata-ddna-tools": ">=0.1.0"
  },
  "peerDependenciesMeta": {
    "deepadata-ddna-tools": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}
